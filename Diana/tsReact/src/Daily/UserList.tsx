/*import { ReactElement, JSXElementConstructor, ReactNode, ReactPortal, Key } from 'react';
import { useSelector } from 'react-redux';

const UserList = () => {
    // Assuming the state structure has a `users` array
    const users = useSelector((state) => state.users.list); // Modify according to your actual state structure

    return (
        <div>
            <h1>Registered Users</h1>
            <ul>
                {users.map((user: { name: string | number | boolean | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReactNode> | ReactPortal | null | undefined; username: string | number | boolean | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReactNode> | ReactPortal | null | undefined; email: string | number | boolean | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReactNode> | ReactPortal | null | undefined; }, index: Key | null | undefined) => (
                    <li key={index}>
                        {user.name} - {user.username} ({user.email})
                    </li>
                ))}
            </ul>
        </div>
    );
};

export default UserList;
*/
